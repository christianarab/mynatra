* insert header
<h1>Posts</h1>
<ul>
    <p> # This feature will iteate over the <%= @name_singular %> saved objects
    <p> # ISSUE: generating an ERB with yet to be created instance variable names
    <p> # TEMP FIX: add code such as:
        
    <% @posts.each do |post| %>
        <li><a href="/posts/<%= post.id %>"><br>Title:<%= post.title %></a></li>
    <% end %>
</ul>
* insert footer